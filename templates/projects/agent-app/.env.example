APP_ENV=development
APP_PORT=8000

# Non-secret: model-runtime hostname, already set as a containerEnv default in
# .devcontainer/devcontainer.json - override here only if different.
OLLAMA_BASE_URL=http://ollama:11434
CHAT_MODEL=gemma3:4b
