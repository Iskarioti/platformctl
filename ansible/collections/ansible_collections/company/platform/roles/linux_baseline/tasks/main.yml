---
- name: Install baseline operational packages
  ansible.builtin.package:
    name:
      - curl
      - jq
      - rsync
    state: present

- name: Ensure time synchronization service is enabled
  ansible.builtin.service:
    name: systemd-timesyncd
    state: started
    enabled: true
  when: ansible_service_mgr == "systemd"
