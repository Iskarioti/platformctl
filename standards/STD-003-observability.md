# STD-003 Observability
Each service must define:
- structured logs
- metrics
- traces where transaction flow crosses services
- health and readiness
- correlation/request identifiers
- dashboards
- alert ownership
- runbook links
Sensitive values, tokens and credentials must never be logged.
