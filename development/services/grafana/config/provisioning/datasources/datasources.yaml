apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://dev-prometheus:9090
    isDefault: true
    editable: false

  - name: Loki
    type: loki
    access: proxy
    url: http://dev-loki:3100
    editable: false

  - name: Tempo
    type: tempo
    access: proxy
    url: http://dev-tempo:3200
    editable: false
