{
  "id": "cadvisor",
  "compose": "compose.yaml",
  "versions": "versions.env",
  "defaults": "defaults.env",
  "secrets": null,
  "secretKeys": [],
  "dependsOn": [],
  "volumes": [],
  "hostEndpoints": [
    {
      "label": "cAdvisor",
      "endpoint": "http://127.0.0.1:${CADVISOR_HOST_PORT}"
    }
  ],
  "dockerEndpoints": [
    {
      "label": "cAdvisor",
      "endpoint": "dev-cadvisor:8080"
    }
  ],
  "notes": "Requires privileged: true and read-only host mounts (docker.sock, /sys, /var/lib/docker) to report per-container resource metrics. This is the standard, documented cAdvisor deployment shape, not a workaround."
}
