# Daily Workflow (deprecated)

Superseded by [`docs/getting-started.md`](getting-started.md), which covers
the same ground more completely and is kept current. This file is left in
place only so old links don't 404 - don't follow its old content, including
the `task bootstrap`/`task dev`/`task verify` commands it references, which
no Taskfile in this repo actually backs.
