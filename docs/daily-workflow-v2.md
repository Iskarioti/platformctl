# Daily Workflow v2 (deprecated)

Superseded by [`docs/getting-started.md`](getting-started.md), which folds in
everything here that's still accurate (`platformctl doctor`, model-runtime
lifecycle, WSL resource-profile switching, the NPU lab, incident diagnostics,
end-of-session steps) and drops the `task lint`/`task type`/`task test`/
`task verify`/`task security` commands this file assumed, which no Taskfile
in this repo actually backs - each template documents its own real lint/test
commands instead.
